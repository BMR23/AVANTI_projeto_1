const columnItems = (num) => Array(num).fill('Categoria');

const titles = ['Categoria', 'Categoria', 'Categoria'];

// const items = titles.map(() => Array(7).fill('Categoria'))
const items = titles.map(() => Array(7).fill('Categoria'));

console.log(items);